apiVersion: v1
kind: Service
metadata:
  name: relay-assistant
  namespace: bootstrap
spec:
  selector:
    app: relay-assistant
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
