apiVersion: v1
kind: Service
metadata:
  name: telemetry-relay
  namespace: bootstrap
spec:
  selector:
    app: telemetry-relay
  ports:
    - name: http
      port: 80
      targetPort: 3001
  type: ClusterIP
