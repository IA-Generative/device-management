# app/services/ — extracted business logic modules from main.py
#
# These modules contain the internal helper functions that were previously
# defined inline in main.py. Routes remain in main.py but delegate to
# these services for business logic.
