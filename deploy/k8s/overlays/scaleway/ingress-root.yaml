apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bootstrap-root
  namespace: bootstrap
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "100m"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - <SCALEWAY_HOSTNAME>
      secretName: bootstrap-tls
  rules:
    - host: <SCALEWAY_HOSTNAME>
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: device-management
                port:
                  number: 80
