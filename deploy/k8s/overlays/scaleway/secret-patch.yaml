apiVersion: v1
kind: Secret
metadata:
  name: device-management-secrets
  namespace: bootstrap
stringData:
  PUBLIC_BASE_URL: "https://bootstrap.fake-domain.name"
  DM_ENROLL_URL: "/enroll"
  KEYCLOAK_ISSUER_URL: "https://bootstrap.fake-domain.name/relay-assistant/keycloak"
