apiVersion: v1
kind: Secret
metadata:
  name: device-management-secrets
  namespace: bootstrap
stringData:
  PUBLIC_BASE_URL: "https://<SCALEWAY_HOSTNAME>"
  DM_ENROLL_URL: "/enroll"
  KEYCLOAK_ISSUER_URL: "https://<SCALEWAY_HOSTNAME>/relay-assistant/keycloak"
