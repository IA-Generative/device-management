apiVersion: v1
kind: ConfigMap
metadata:
  name: device-management-config
  namespace: bootstrap
data: {}
# Plugin configs are now stored in the database (plugins.config_template JSONB).
# This ConfigMap is kept as a placeholder for potential future generic DM settings.
# Legacy plugin entries (matisse-config.json, libreoffice-config.json, etc.)
# were removed — see legacy-config-backup.tar.gz for archive.
